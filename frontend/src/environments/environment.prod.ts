export const environment = {
  production: true,
  // Replace with real deployed service URLs at build/deploy time
  // (e.g. via Railway environment variable substitution in CI).
  userServiceUrl: '/api/user-service/api/v1',
  farmTwinServiceUrl: '/api/farm-twin-service/api/v1',
};
